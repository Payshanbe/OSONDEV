# Copy to .env.local for local dev. Add the same keys in Vercel → Environment Variables.

# Admin panel (/admin) — required in production
ADMIN_PASSWORD=your-secure-password
ADMIN_SECRET=your-random-secret-at-least-32-chars

NEXT_PUBLIC_SITE_URL=https://your-project.vercel.app

# GitHub CMS (Vercel production saves)
GITHUB_TOKEN=github_pat_xxxxxxxx
GITHUB_REPO_OWNER=Payshanbe
GITHUB_REPO_NAME=OSONDEV
GITHUB_BRANCH=main

# Vercel Blob — work cover uploads (create store in Vercel → Storage → Blob)
BLOB_READ_WRITE_TOKEN=vercel_blob_rw_xxxxxxxx
